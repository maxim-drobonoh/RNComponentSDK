{
  "name": "rn-component-sdk",
  "version": "1.0.0",
  "private": true,
  "description": "iOS SDK that wraps React Native components with clean Swift API",
  "scripts": {
    "setup": "npm install && npx install-expo-modules@latest && pod install",
    "postinstall": "echo 'Dependencies installed. Run: npm run setup to configure iOS'"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-native": "0.81.4",
    "expo": "~49.0.0",
    "expo-modules-core": "~1.5.0",
    "expo-font": "~11.4.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}

